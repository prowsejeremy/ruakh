services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ruakh
      POSTGRES_PASSWORD: ruakh
      POSTGRES_DB: ruakh
    ports:
      - "5432:5432"
    volumes:
      - ruakh_pgdata:/var/lib/postgresql/data

volumes:
  ruakh_pgdata:
