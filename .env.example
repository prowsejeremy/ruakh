DATABASE_URL=postgres://ruakh:ruakh@localhost:5432/ruakh

# Web Push (generate with: npx web-push generate-vapid-keys)
# Optional in dev — without keys the app runs and the reminder scheduler stays idle.
PUBLIC_VAPID_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
